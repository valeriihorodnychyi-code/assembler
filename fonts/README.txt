Put your .ttf / .otf font files here.

The "font_name" field in a style JSON (and the editor's "Font file" upload)
refers to a file in this folder, e.g. "Arial Black.ttf".

If a font isn't found here, the renderer falls back to a default font, so the
app still runs — but for brand-correct captions, drop your real fonts in.
