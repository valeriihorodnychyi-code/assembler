fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
Pillow>=10.0
faster-whisper>=1.0.0
requests>=2.31
certifi>=2024.2.2      # CA bundle so the frozen-app auto-updater can verify HTTPS to GitHub
