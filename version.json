{
  "version": "1.9.18",
  "channel": "main",
  "notes": "Export button on the captions screen (pulses for one video; a pack pulses Assemble instead). Per-phrase style — give any caption its own preset. Scrim now fades in/out and no longer pops with the first caption. Second word shadow. Drag the caption up/down on the preview. Style→all restored. Presets cleaned up: defaults come from styles/*.json (no star, editable on GitHub), customs are mirrored locally so a Drive wipe can't lose them."
}
