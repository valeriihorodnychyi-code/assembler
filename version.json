{
  "version": "1.9.20",
  "channel": "main",
  "notes": "Fix: the UI no longer shakes during playback. The time counter under the preview had an auto width, so its number changed width every frame (e.g. 9.9s → 10.0s) and nudged the whole centred layout. It now has a fixed width with tabular figures, and the preview canvas only resizes on a format change (not every frame)."
}
